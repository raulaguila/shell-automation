# Configure Tilix terminal on Ubuntu

Install tilix

```shell
sudo apt update && sudo apt install tilix
```

Execute to change default terminal:

```shell
sudo update-alternatives --config x-terminal-emulator
```

Press the Tilix index and press <ENTER>.
